// Calculate "Simple interest" from Principal(p), Rate(R) & Time(T)
// SI = P * R * T

function calculateInterest(p, r, t) {
  let si = p * r * t;

  return si;
}

console.log(calculateInterest(10, 12, 3));
